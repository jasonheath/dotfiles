-- the advantage of this plugin over others is that it silently strips out
-- trailing whitespace on lines that you are changes and ignores trailing
-- linespaces on lines you haven't
return { 'axelf4/vim-strip-trailing-whitespace' }

